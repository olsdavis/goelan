package permission

const (
	BanPermission  = "ban"  // allows to ban players
	BasePermission = "base" // all the basic permissions (essentially basic commands)
	StopServer     = "stop" // allows to stop the server
)
